<h1><%= @city.name %></h1> 


<div class="card-columns">
    <% @city.users.each do |user| %>
    <% user.gossips.each do |gossip|%>

      <div  class="card text-white bg-secondary mb-3 " style="max-width: 20rem;">
        <div class="card-header"><%=gossip.title %><br/></div>
        <div class="card-header"><%=gossip.content %><br/></div>

        <div class="card-body">
          <h5 class="card-title"><%= user.first_name %><br/></h5>
          <p class="btn btn-warning"><%= link_to "Détails", "/gossips/#{gossip.id}" %></p>
       </div>
      </div>

    <% end %>
    <% end %>
  </div>